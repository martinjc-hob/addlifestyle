name: Build Mac App

on:
  push:
    branches: [main]

jobs:
  build-mac-arm:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pyinstaller pillow pandas requests openpyxl pyarrow tkinterdnd2

      - name: Build app
        run: |
          pyinstaller --onefile --windowed --name "Add Lifestyle" addlifestyle.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AddLifestyle-Mac-AppleSilicon
          path: dist/Add Lifestyle.app

  build-mac-intel:
    runs-on: macos-13-xlarge
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pyinstaller pillow pandas requests openpyxl pyarrow tkinterdnd2

      - name: Build app
        run: |
          pyinstaller --onefile --windowed --name "Add Lifestyle" addlifestyle.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AddLifestyle-Mac-Intel
          path: dist/Add Lifestyle.app